# Chem-Assembler 開発方針

本ドキュメントは、Chem-Assembler（有機化学構造パズル）の開発における設計原則・コーディング規約・品質保証手順・作業ロードマップを定める。以後の修正・機能追加はすべて本方針に従って行う。

---

## 1. プロジェクトの目的と開発コンセプト

- 高校〜大学初級の有機化学学習者が、分子構造（トポロジー・結合次数・不斉炭素）を手で組み立てて理解するためのブラウザゲーム。
- **「化学的に正しい答えは必ず正解になる」ことを最優先の品質基準とする。** 誤検証（正しいのに不正解）はゲームとして最も致命的な欠陥であり、他のあらゆる作業に優先して修正する。

### 1.1 開発コンセプト（作図思想）

本アプリは **立体模型の忠実再現ではなく、紙に手書きするのと同じ感覚での構造式作図** を目指す。

1. **手書き感覚の優先**: 学習者がノートに構造式を書くときの感覚（直線・直角中心の作図）を再現する。例えばアルカンは、実際の結合角（109.5°）を反映したジグザグの折れ線ではなく、**直交方向（上下左右90°）に作図するのが本アプリの正**である。
   - したがって、直交グリッドスナップ・90°方向の水素配置は「不正確さ」ではなく**意図された仕様**であり、リアルな結合角に近づける方向の「修正」を行ってはならない（過去にsp3自動レイアウトがドリフトバグを生んで撤去された経緯がある）。
   - 例外として、構造式の慣習に従い sp2（C=C、ベンゼン環）は120°、sp（C≡C）は180°の作図を用いる。これは紙の上の構造式でも同様に書かれるためである。
2. **反応機構アニメーションへの発展**: 将来的に、巻矢印（曲がった矢印）による電子の動き・結合の組み換えをアニメーションで再現し、反応機構の学習をサポートすることを目指す。データモデル（原子・結合のグラフ構造）はこの発展を妨げない形を保つ。
3. **立体模型との対照**: 作図はあくまで平面の構造式としつつ、「作図上ではこう書くが、実際の分子はこうなっている」という対照提示（立体表現の併記・比較ビュー等）ができるとなおよい。平面作図の正しさと立体的な実像を混同させない見せ方を将来検討する。

## 2. アーキテクチャ原則

| ファイル | 責務 | 禁止事項 |
|---|---|---|
| `chemistry.js` | 化学モデル（原子・結合・分子・価標・同型判定・不斉判定）。**DOMに依存しない純粋ロジック** | DOM操作・描画・イベント処理を書かない |
| `game.js` | UI・描画（SVG）・入力処理・ステージ進行 | 化学的な判定ルールを直接実装しない（chemistry.jsに委譲） |
| `stages.json` | 問題データ（純粋データ、コメント不可） | ロジックを持ち込まない |
| `index.html` / `style.css` | 構造とスタイル | インラインstyleの新規追加は避け、style.cssに寄せる |

- 外部ライブラリ・ビルドツールは導入しない（vanilla JS + 静的配信を維持。GitHub Pages 互換）。
- 定数は一元管理する。価標は `VALENCIES`（chemistry.js）を唯一の情報源とし、重複定義（例: 旧 `getMaxBondType` 内のswitch）を作らない。

## 3. コーディング規約

### 3.1 文字エンコーディング（最重要）

- **すべてのファイルは UTF-8（BOMなし）で保存する。** 過去に Shift-JIS との二重変換でコメント・UI文字列が広範囲に文字化けした事故があるため、以下を徹底する:
  - コミット前に、変更ファイルに文字化けパターン（`縺`・`繧`・`繝`・`蜊`・`荳`・`邨`・`蛾`・`倡` 等の頻出化けクラスタ）が含まれていないか確認する。v104でこのパターン外の化け（`蜊倡ｵ仙粋`＝単結合）を発見したため検査文字を拡充した。
  - ユーザーに表示される日本語文字列（alert/confirm/textContent/innerHTML）を変更した場合は、必ずブラウザで実表示を確認する。
- 既存の文字化けコメントは、修正対象ファイルに触れる際に随時正しい日本語へ復元するか削除する。

### 3.2 スタイル

- 命名・コメント密度は既存コードに合わせる（コメントは日本語）。
- デッドコードは残さない。「無効化した関数」を空実装で残す場合は、呼び出し側ごと削除するか、残す理由をコメントで明記する。
- `substr` 等の非推奨APIは新規コードで使わない（既存は触るついでに置換）。

### 3.3 座標変換の原則

- **クライアント座標 ⇔ SVG座標の変換は必ず `svg.getScreenCTM().inverse()`（または `createSVGPoint().matrixTransform`）で行う。** `viewBox.width / rect.width` のような手計算は、`preserveAspectRatio` によるレターボックスを考慮できずズレを生むため禁止する。
- viewBox の操作（ズーム・パン・フィット）は既存方式を維持してよいが、座標の読み取りは上記に統一する。

### 3.4 入力処理の原則

- マウス・タッチの入力は **Pointer Events（`pointerdown`/`pointermove`/`pointerup`/`pointercancel`）に統一**することを目標とする。移行完了までの暫定対応でも:
  - タッチ系ハンドラでフェイクイベントを作る場合、**必ず座標（`changedTouches[0]` 等）を含める。** 座標なしイベントを座標依存の処理に渡さない。
  - 合成マウスイベントの二重発火を防ぐため、タッチを処理したら `preventDefault()` する。
- `alert` / `confirm` / `prompt` は原則使わない。通知は `#verify-result` のトースト表示、確認・入力はモーダルで行う。（既存の `window.onerror` の alert はデバッグ残骸であり撤去対象。）

### 3.5 Undo の原則

- `saveState()` は **実際に状態が変化する操作の直前のみ** 呼ぶ。空振り操作（何も消えない消しゴムクリック等）で履歴を消費しない。
- 状態の保存・復元は **原子の全プロパティ**（`isAsymmetricMarked`、`benzeneCenter`、`benzeneAngle` を含む）を対象とする。復元漏れを防ぐため、ホワイトリスト式のプロパティコピーではなく、シリアライズしたオブジェクトからの機械的復元を優先する。

## 4. 化学モデルの原則

1. **正解データ（stages.json）は、アプリ自身の価標ルールで構築可能でなければならない。** ユーザーが通常操作（原子配置・結合ツール・モジュール・結合次数トグル）で到達できない正解データを置かない。
2. **モジュール（官能基ボタン）が生成する構造は、そのモジュールを想定するステージの正解データと結合次数まで一致させる。** （例: -NO₂ モジュールは N(=O)(–O) を生成する。N(=O)(=O) は価標超過であり禁止。）
3. **芳香環のケクレ位相（単結合・二重結合の位置）の違いは正誤に影響させない。** 6員環で単・二重結合が交互になっている環は「芳香環」として扱い、検証時に環内結合の次数差を吸収する。
4. **二重結合の幾何（シス/トランス）は例外的に座標から読み取る。** 座標は原則「見た目専用」だが、C=C まわりの置換基の同側/反対側は2D構造式が幾何異性を伝える標準的な手段であるため、**命名表示（および将来の幾何指定ステージ）に限り**座標を参照する。対象は「環に含まれないC=Cで、両端の炭素がそれぞれ重原子置換基をちょうど1個持つもの」。直線に描かれた場合は「未指定」として扱い、通常のステージ正誤判定は引き続きトポロジーのみで行う。
5. 価標は `VALENCIES` を唯一の情報源とする。多価元素（S: 2/4/6 等）への拡張を行う場合は、モデル・検証・自動水素補完の三者を同時に対応させる。それまでは S を使う出題はスルホ基（6価）に限定する。
6. 自動水素補完は「描画とH数検証の一貫性」を守る。ユーザーがHを明示配置する機能は追加しない（現行仕様の維持）。

## 5. UI/UX の原則

### 5.0 作図スタイル（コンセプト1.1の具体化）

- スナップ・自動配置・自動水素は **直交（90°）を基本** とし、sp2は120°、spは180°とする。結合角の立体的リアリティを理由にこれを変えない。
- 検証ロジックは作図上の座標・角度に依存させない（トポロジーと結合次数のみで判定する現行方式を維持）。座標は見た目のためだけの情報とする。

- **ヘルプ・ヒントの記述と実装は常に一致させる。** 操作方法を変えたら同一コミットで `index.html` のヘルプを更新する。実装されていない操作をヘルプに書かない。
- 削除系の操作は影響を最小にする。**削除の影響は削除対象の原子・結合のみに限定し、連結が切れて孤立した原子を自動削除（巻き添え削除）しない。** 紙の上で消しゴムをかけたのと同じ挙動とする（コンセプト1.1-1）。孤立した原子が残ることは正常であり、ユーザーが自分で消すか再結合する。
- 判定結果メッセージは実際に検証した内容だけを述べる（不斉モードOFF時に「不斉炭素が一致」と表示しない）。
- 非表示中の機能（お手本表示・化学式・説明文）は、方針が決まるまで削除しない。ただし到達不能コードであることをコメントで明示する。

## 6. 品質保証（修正ごとの検証手順）

すべての修正は、コミット前に以下を実施する:

### 6.1 回帰テスト（必須）

1. ローカルサーバー（例: `python -m http.server 8123`）で起動し、コンソールエラーがないこと。
2. **`test.html` を開き、全テストが合格することを確認する。** 過去に修正した不具合（座標変換・ケクレ・削除・伸縮・振り分け・反応ビューア等）の再発を自動検出する回帰テストページであり、修正内容に関わらずコミット前に必ず実行する。
3. 新しい不具合を修正したときは、その再現ケースを `tests.js` にテストとして追加する。

### 6.2 回帰チェックリスト（該当領域を修正した場合）

- **座標系**: ウィンドウを縦長・横長にリサイズし、キャンバスの四隅付近でプレビュー（ゴースト）がカーソル直下に出ること。ズーム・パン後も同様。
- **検証ロジック**: 以下をブラウザコンソールで実行し、期待値になること。
  - ニトロベンゼン: -NO₂ モジュールで組んだ構造が正解になる。
  - o-キシレン: ベンゼン環モジュールの単結合側・二重結合側どちらにメチル基を付けても正解になる。
  - 酢酸: C-C-(=O)(-OH) が正解、C-C-(-O)(-OH)（次数違い）が不正解のまま。
- **Undo**: 不斉マーク → Undo → マークが復元される。配置 → Undo → 配置前に戻る。
- **削除**: プロパン中央Cの削除で両端のCが残る。
- **タッチ（対応時）**: タップ配置が1回で確定する（配置→即削除にならない）。タッチドラッグで結合が張れる。

### 6.3 検証の記録

- 各修正のコミットメッセージ本文に「何をどう検証したか」を1行以上書く。

## 7. バージョン管理・リリース手順

- コミットは **1修正=1コミット**。メッセージは既存の Conventional Commits 形式（`fix:` / `feat:` / `chore:`）を踏襲し、末尾に `(vNN)` を付ける。
- バージョン番号 `vNN` を上げる際は、**`index.html` と `test.html` の全キャッシュバスターを同一コミットで** 更新する（不一致はキャッシュ事故のもと。GitHub Pagesはmax-age=600で配信するため、バスターのないURLは最大10分間古い版が使われ続ける）:
  1. `index.html` の `style.css` / `chemistry.js` / `game.js` / `reaction.js` / `quiz.js` の `?v=NN`
  2. `index.html` ヘッダー表示 `<div class="version">vNN</div>`
  3. `test.html` の `index.html?v=NN`（iframe）と `tests.js?v=NN`
  4. `audit.html` の `index.html?v=NN`（iframe）と `audit.js?v=NN`（v108で追加）
  - 一括更新例: `sed -i 's/?v=OLD/?v=NEW/g' index.html test.html audit.html` ＋ ヘッダー表示の置換
  - **JSONデータの fetch には必ず `{ cache: 'no-cache' }` を付ける**（URLにバスターが付かないため。v125で tutorials.json だけ付け忘れ、更新が反映されない事象が発生した）。
  - **開発中にブラウザで検証する前にも、コードを変更したらバージョンを上げる。**同一 `?v=NN` のまま再読み込みしても旧版がキャッシュから配信され、「実装したのに動かない」と誤診する（v123・v125で2回発生）。
- main ブランチ直コミット運用を維持する（単独開発のため）。ただし大きなリファクタ（Pointer Events 移行等）はブランチを切ってもよい。

## 8. ロードマップ（優先順位順）

レビュー（2026-07-16 実施）で確認された問題を、以下の順序で修正する。各項目は完了時にチェックする。

### フェーズ1: 誤検証の根絶（最優先）

- [x] **P1-1** 座標変換を `getScreenCTM()` ベースに置換（クリック/プレビューの上下端ズレ最大±37px の解消）— v57
- [x] **P1-2** -NO₂ モジュールの結合次数を N(=O)(–O) に修正（ニトロベンゼン・ピクリン酸が通常操作でクリア可能に）— v58
- [x] **P1-3** 芳香環のケクレ位相を検証で吸収（オルト二置換6ステージの理不尽な不正解を解消）— v59

### フェーズ2: データ破壊系バグ

- [x] **P2-1** Undo で `isAsymmetricMarked` が消える問題の修正（状態復元の機械化）— v60
- [x] **P2-2** 中央原子削除で分子全体が消滅する問題の修正（巻き添え削除の廃止。削除の影響は対象のみに）— v61
- [x] **P2-3** 空振り操作での `saveState()` 抑止（消しゴム空クリック・モジュール接続先なし・空の全消去・移動なしドラッグ）— v62

### フェーズ3: モバイル入力

- [x] **P3-1** `touchend` の座標欠落修正 — P3-2のPointer Events統一に包含（pointerupは常に座標を持つ）— v63
- [x] **P3-2** Pointer Events への統一（タップ二重発火・タッチドラッグ不能の根本解決。touch-action:none併用）— v63

### フェーズ4: 整合性・クリーンアップ

- [x] **P4-1** ユーザー可視の文字化け修正（Delete確認ダイアログ）と、化けコメントの一掃（自動逆変換14行＋手動書き直し117行）— v64
- [x] **P4-2** ヘルプと実装の整合 — 原子の右クリック削除を実装（右ドラッグはパンのまま、移動量3px以下で削除と判別）— v65
- [x] **P4-3** `window.onerror` の alert 撤去（#verify-resultトースト＋console出力へ）— v66
- [x] **P4-4** デッドコード削除（`autoLayoutBonds` 空実装と呼び出し7箇所、`belongsToSp2SpTree`、`translateSubtree`）、`getMaxBondType` の `VALENCIES` 一元化 — v67
- [x] **P4-5** 背景グリッド 60px → `GRID_SIZE`(42px) に一致させる — v68
- [x] **P4-6** 判定成功メッセージの文言を検証内容に合わせる（不斉モードON/OFFで出し分け）— v69
- [x] **P4-7** `renderTargetBond` の三重結合描画対応（お手本機能復活の前提）— v70

### フェーズ5: 機能・出題の拡充（方針決定後）

- [x] **P5-1** お手本表示・化学式・説明文をすべて復活（ユーザー決定 2026-07-16）— v71
- [x] **P5-2** 不飽和化合物ステージの復活 — 新シリーズ「不飽和化合物（多重結合）」7問＋アラニン（不斉炭素練習用）追加。stages.json のキャッシュ再検証も強制化 — v73
- [x] **P5-3** S出題の制限をUIに明記（6価=スルホ基用。パレットのツールチップ＋ヘルプに注意書き）。多価対応は将来課題 — v74
- [x] **P5-4** ベンゼン環原子の元素置換対応（価標チェック付き）＋ピリジンステージ追加 — v75
- [x] **P5-5** 反応機構アニメーション基盤の設計（→ [DESIGN_reaction_mechanism.md](DESIGN_reaction_mechanism.md)）
  - [x] **P5-5-M1** reactions.json スキーマ＋ローダー＋静的表示＋巻矢印静的描画（エテン+Br₂、ステップ送り・形式電荷表示付き）— v76
  - [x] **P5-5-M2** ステップ再生アニメ（矢印描画＋状態補間＋▶/⏸コントロール。非表示タブではsetTimeoutフォールバック）— v77
  - [x] **P5-5-M3** 代表反応データ追加＋片羽矢印 — 完了（計4反応収録）
    - [x] 片羽矢印・ラジカル(•)表示・mid矢印（生成中の結合を指す）対応＋メタンの塩素化（開始/成長/停止の3ステップ）— v78
    - [x] エステル化（酢酸＋エタノール、簡略2段階・電荷付き中間体）— v79
    - [x] ベンゼンのニトロ化（アレニウムイオン中間体を経る求電子置換）— v79
  - [x] **P5-5-M4** 生成物予測モード（主生成物をパズルUIで組み立て、明示水素除去＋最大連結成分のターゲットに対して verifyMolecule 判定。パズル作業分子は退避/復元）— v80
- [ ] **P5-6** 立体模型との対照ビューの検討（平面構造式と実際の立体構造の比較提示。コンセプト1.1-3）

### フェーズ6: フィードバック対応

- [x] **P6-1** 原子配置のプレビューと実結合の不一致を解消（2026-07-17 ユーザー報告）。プレビューと配置が同一の判定関数 `getPlacementBondTargets` を共有する構成に変更し、複数原子と隣接できる位置では可能な結合をすべて形成する仕様（案2）を採用（環を閉じられる。不要な結合は右クリック削除＋deletedBonds履歴で解除可能）— v81
- [x] **P6-2a** 重なりを避けられない場合の原子配置を禁止（noSpaceフラグ＋案内トースト）— v82
- [x] **P6-2b** 結合の伸縮操作（2026-07-17 ユーザー提案）。結合線を軸方向にドラッグするとグリッド倍数で伸縮。橋判定（結合を除いた連結性）で環内結合は伸縮不可・環は剛体として移動、伸縮方向は結合の現在の軸（ナナメ結合はナナメのまま）、静止側と重なる長さは採用しない、伸縮ではautoConnectを適用しない — v83
- [x] **P6-3** 環への側鎖の振り分け配置（2026-07-17 ユーザー提案）。側鎖1本目=外向き二等分線、2本目=二等分線±30°で、既存側鎖が二等分線上なら枝ごと反対側へ平行移動（回転ではないためその先の直交レイアウトは不変）。環結合/側鎖の区別は橋判定。移動先が塞がっていれば移動しない。プレビューに移動先ゴースト表示。環⇆鎖の切り替え時は既存原子を動かさず、判定は常に配置時点の環所属で行う — v84

- [x] **P6-4** 結合クリックの次数トグルが効かない退行を修正（2026-07-20 ユーザー報告）。v83の伸縮機能で、純クリック時にもupdateDrawingが走ってヒットライン要素が再生成され、clickイベントが届かなくなっていた。純クリック時は再描画しないよう修正し、「クリック後も要素がDOMに残る」ことを検証する回帰テスト（C9）を追加 — v86

- [x] **P6-5** 三重結合の端の自動水素を180°直線配置に修正（2026-07-21 ユーザー報告）。calculateHydrogensに三重結合(sp)の分岐がなくsp3扱い（90°）になっていた。H–C≡C–H の手書き慣習どおり結合の反対側に配置し、回帰テストB7を追加 — v87

### フェーズ7: 品質基盤・公開・コンテンツ制作体制（2026-07-20 方針合意）

- [x] **P7-1** ブラウザ回帰テストページ（test.html + tests.js）— 実アプリをiframeで駆動する26テスト（基盤/検証ロジック/編集操作/伸縮・振り分け/反応ビューア/エクスポート）。6.1章のコミット前チェックを1クリック化
- [x] **P7-2** GitHub Pages で公開 — 既に有効であることを確認（公開URL: https://o9o4oo78o42.github.io/ChemistryGame/ 。main へのプッシュで自動デプロイされる。test.html も同URL配下で動作する）
- [x] **P7-3** 作図エクスポート機能 — キャンバス上部の「⤓ JSON」ボタンで現在の分子を stages 用（target: 重原子のみ）＋ reactions 用（withHydrogens: 自動水素を明示原子化）のJSONとしてクリップボードへコピー — v85
- [x] **P7-4** 小粒の学習体験改善: ①クリア状況のlocalStorage保存＋ステージ一覧の✓表示 ②Redo（Ctrl+Y / Ctrl+Shift+Z。新操作でRedo履歴破棄） ③alert/prompt撲滅（任意員環は員数選択モーダル、官能基の案内はトースト） ④不斉マーク誤りは座標文字列ではなくオレンジ点線円の原子ハイライトで提示 — v96
- [x] **P7-5** 立体対照ビュー（P5-6）の設計書作成 → [DESIGN_3d_correspondence.md](DESIGN_3d_correspondence.md)（M1: sp3炭素の局所くさび図／M2: 全体の標準くさび描画／M3: 疑似3D回転とプリセット配座。実装は設計合意後）
  - [x] **P7-5-M1** sp3炭素の局所くさび図モーダル（stereo.js）— 「🧊立体で見る」→キャンバスでsp3炭素をクリック→くさび形表記（紙面内2本＋手前くさび＋奥ハッシュ）と置換基ラベル（OH/CH₃/組成式）を自動生成。「作図は90°・実際は109.5°の正四面体」の教育文言と不斉判定の説明（不斉なら鏡像異性体、非不斉なら同一置換基の指摘）を連携表示 — v98
  - [ ] **P7-5-M2** 分子全体の標準くさび描画（正準レイアウト）
  - [ ] **P7-5-M3** 疑似3D回転とプリセット配座（いす形など）
- [x] **P7-6** 化合物名判定 — 右パネルに「いま描いている分子」カードを新設し、名称（stages.json 57件＋compounds.json 24件とのグラフ同型照合。ケクレ位相も吸収）と分子式（自動水素込み・Hill表記）をライブ表示。生成物予測モード中は名称を伏せる。系統名（IUPAC）アルゴリズムは将来検討 — v88
- [x] **P7-8** 縮合環の作図サポート — (1) 環モジュールの配置を配置計画方式（getRingPlacementPlan）に改修: カーソルが「既存結合を1辺とする正N角形の中心」40px以内なら縮合位置に吸着（辺の向き・長さ任意。五員環×六員環などの異種縮合も可）、それ以外は絶対グリッド丸めの自由配置。ホバー中に環のゴーストを表示（吸着頂点=白抜き、配置不可=赤）し、プレビュー＝実配置を保証。マージされない新規頂点が既存原子27.3px以内なら配置拒否（環の重なり事故防止）。ベンゼンの二重結合は貪欲割り当てで縮合してもケクレ交互を維持（ナフタレン対応） — v102 (2) 直交作図の環（環の隣接2方向がともに水平/垂直）では±30°振り分けではなく格子方向へ配置し、手描きの縮合環構築（長方形六員環→隣接環→中央結合の切断）を可能に — v103。テストJ1（ナフタレン・デカリン・重なり拒否）/J2（手描きデカリン骨格）追加。実クリック検証: ベンゼン2クリック→ナフタレン、六員環+五員環→ヒドリンダン
- [x] **P7-10** バグ探索パス第2弾（クイズ・反応・命名の網羅監査） — 全109化合物×3強度×2回の表記変形で同型性・正準コード安定性を機械検査（654件合格）、幾何指定4エントリのシス/トランス保存（30件合格）、命名クイズ全シリーズ×全強度で72問生成（選択肢重複・正解欠落なし）、反応9機構の前進/後退/再再生とコンソールエラー監視（クリーン）。発見: ニトロ基の単結合Oに自動水素が付き、ニトロベンゼンの分子式が C₆H₆NO₂ と誤表示（画面上も -N(=O)-OH に見えていた）→ getFreeValency にニトロ特例（N使用価標4以上＋N=Oを持つNの単結合Oは空き価標0）を追加して修正。テストK1 — v106
- [x] **P7-9** 官能基モジュールの計画方式化 — 配置ロジックを getFunctionalGroupPlan に分離し、ホバーで接続先原子に配置予定のゴースト（結合線＋元素記号）を表示。プレビュー＝実配置を保証。接続先の空き価標なし／新規原子が既存原子と重なる（27.3px未満）配置は拒否してトースト案内（Undo履歴非消費）。テストJ3（ゴースト表示・計画と実配置の一致・酢酸 C₂H₄O₂・価標拒否）追加 — v105
- [x] **P7-7** タッチパネル対応の堅牢化 — タッチエミュレーション検証で発見した実バグを修正: (1) ピンチ開始時、1本目の指のpointerdownが行った編集（原子配置・伸縮の履歴積み）をスナップショットから巻き戻す（trackPointerDown共通前処理）(2) 結合ヒットライン上から始まるタッチもピンチ判定に参加（従来は2本指と認識されず）(3) ピンチ開始でbondStretchもキャンセル。結合タップの次数トグルは合成clickに依存するためヒットライン側はpreventDefaultしない。テストI1（巻き戻し・履歴クリーン・結合上ピンチ・タッチ伸縮）追加。実機タッチパネルでの確認は未了（ユーザーがデバイス入手後に実施予定） — v101

### フェーズ8: 表記の同値性を学ぶ（2026-07-21 方針合意。「別の描き方でも同じ化合物」の理解を学習目標化）

- [x] **P8-1** シス/トランスの判定と命名区別 — `getDoubleBondGeometry`（環外C=C・2置換のみ対象、外積の符号で側を判定、直線描画は不定）を新設し、命名照合で幾何指定付きエントリを優先。シス-2-ブテン／トランス-2-ブテン／2-ブテン（未指定）を描き分けで区別 — v89
- [x] **P8-2** 正準形基盤 — `canonicalCode`（WL精緻化＋最小行配列のタイブレーク探索。連結成分ごとに正準化してソート結合＝同一成分の繰り返しによる組合せ爆発を防止）。同値関係はverifyMoleculeと同一（重原子＋自動H数＋結合次数、ケクレ吸収、立体は区別しない）。名称判定をコードMapのO(1)照合＋verify最終確認に変更。不斉判定を根付き正準コード（`rootedFragmentCode`）による厳密比較へ置換し、環近似だった旧serializeSubtreeを削除。性質テスト（全ライブラリの原子順シャッフル不変・全ペアのコード一致⇔同型・ケクレ不変・非連結ケース）で防御 — v95
- [x] **P8-3** 「同じ化合物？」クイズモード（quiz.js）— 「同じ」問題は1化合物を表記変換（90°回転・反転・ケクレ位相反転・橋結合の伸長。すべてトポロジー不変）して並置、「違う」問題は同分子式・別トポロジーの異性体ペア（自動抽出）を並置。正誤の正はverifyMolecule。解説で名称・分子式・「つながり方が同じなら同じ化合物」を提示。データ作成は不要（既存ライブラリ81件から自動生成）— v90
- [x] **P8-4** 命名クイズモード — 意図的に正準形でない図（表記変換を1〜2回適用）を提示して名称を4択で回答。誤答選択肢は同分子式の異性体名を優先して自動生成（不足時は他の名前で補完）。同一トポロジーで別名のエントリ（2-ブテン系）は正解が一意に決まらないため出題対象外。表記変換・描画・ライブラリ構築はP8-3と共有ヘルパー化 — v91
- [x] **P8-5** クイズ調整（2026-07-21 フィードバック）: ①出題範囲のシリーズ絞り込みドロップダウン（compounds.jsonは「その他の有名化合物」として選択可）②崩し方の強度3段階（弱=回転・反転のみ／標準／強=ケクレ反転必須＋伸長2回・最大84px。命名クイズは強で変換2回）③回答解説に「構造のポイント」（describeStructureによる骨格・多重結合・官能基の自動要約。正解時にも表示、「違う」問題では左右それぞれを表示）— v92
- [x] **P8-6** 命名可能化合物の範囲拡張（第1弾）— ソースは「高校化学（検定教科書）の有機分野の定番」。アルコール異性体（1-/2-ブタノール・tert-ブチルアルコール）、カルボニル系（プロパナール・ブタノン・ベンズアルデヒド）、酸・エステル系（アクリル酸・酢酸ビニル・無水酢酸・アジピン酸・フタル酸・テレフタル酸）、マレイン酸／フマル酸（幾何指定付き＋未指定名）、アミノ酸（グリシン）、ナイロン原料（ヘキサメチレンジアミン）、ハロゲン化物（クロロメタン・1,2-ジブロモエタン・1,1-/1,2-ジクロロエタン・クロロベンゼン）、芳香族（エチルベンゼン・クメン・アセトアニリド・TNT）、酸化エチレン等の27種を追加し計53種に。以後の拡充は⤓JSONエクスポートで随時 — v97
- [ ] **P8-7** 反応機構コンテンツ拡充（教科書定番の候補リスト: エテンへの水付加／エタノールの分子内脱水→エテン・分子間脱水→ジエチルエーテル／アルコールの酸化→アルデヒド→カルボン酸／エステルのけん化／ベンゼンのスルホン化・塩素化／アニリンのジアゾ化とカップリング。リクエストに応じて順次データ作成）
  - [x] エテンへの水付加（酸触媒・3ステップ: π攻撃→カルボカチオン→オキソニウム→脱プロトンで触媒再生）／エタノールの分子内脱水（逆反応の対・E1: プロトン化→水の脱離→β脱離）／酢酸エチルのけん化（OH⁻攻撃→四面体中間体→エトキシド脱離→酸塩基でカルボン酸塩に）を追加。全機構の価標・添字を機械検証するテストE4も追加（計7機構） — v99
  - [x] ベンゼンのスルホン化（SO₃の求電子攻撃→アレニウム→O⁻がH⁺を回収）／塩素化（分極したCl₂の攻撃→アレニウム→Cl⁻がH⁺を回収してHCl）を追加（計9機構）。E4は登録済み全機構の通し再生に拡張 — v100
  - 残り候補: アルコールの酸化（酸化剤の扱いが高校範囲では機構化しにくいため保留）／エタノールの分子間脱水／アニリンのジアゾ化とカップリング（多段・要設計）

### フェーズ9: 反応実行モードと学習の深化（2026-07-22 ユーザー要望を登録。優先順は都度相談）

- [x] **P9-1** 反応実行モード（M1〜M5・検収・フォローすべて完了につき2026-07-24クローズ） — 設計書: [DESIGN_reaction_execution.md](DESIGN_reaction_execution.md)（2026-07-22 作成・ユーザー確認待ち）。ユーザーが分子を1つ以上作成（または名称から呼び出し）→「この分子の反応」→反応種を選択（酸化／付加／置換／エステル化／脱水縮合…）→反応が進行して生成物に変化する。反応表示は「簡易版（前後モーフィング）」⇔「機構版（登録済み巻矢印機構の再生）」切替。ルールベース（detect/apply）＋官能基検出（chemistry.jsに純粋関数で追加）＋試薬パレット。M1: 名称呼び出し＋プロパティカード / M2: 単分子反応（酸化・脱水） / M3: 二分子反応（エステル化等） / M4: 付加・けん化・機構版連携 / M5: 芳香族置換ほか
  - [x] **P9-1検収フォロー**（ユーザー方針: 進行しにくい反応は「除外」でなく「原理的には可能だが…」と説明し、無水酢酸アセチル化へ誘導する教育スタイル）— 酢酸＋フェノールで「⚠エステル化（フェノールは進行しにくい）」解説ボタンを表示（共役による低反応性・平衡の偏りを説明し、アセチル化ボタンへ誘導。分子は不変・Undo非消費）。実行可能な「アセチル化（無水酢酸）」ルールを新設: フェノール性OHとアミンのNH₂にアセチル基CH₃CO-を付加（かたまり衝突判定つき配置）。アニリン→アセトアニリド、サリチル酸→アセチルサリチル酸（アスピリン）を名称判定で確認。learn.jsのフェノール学習ポイントにも同説明を追加。テストL6更新＋L7 — v118
  - [x] **P9-1検収**（2026-07-22 Fableによる差分レビュー・モデル分担ルールの「実装と検収の分離」の初回実施）— v111〜v115（M3〜M5・異性体列挙・監査修正）をレビュー。プローブで実バグ2件＋化学的不正確1件を発見し修正: (1) アルキンへの水付加がエノール（C=C-OH）のまま停止 → ケト・エノール互変異性でケト形を直接生成（アセチレン→アセトアルデヒド、プロピン→アセトン[マルコフニコフ則で内側Cに=O]）(2) エノールに酸化ルールが適用でき、ケテン CH₂=C=O（高校範囲外の異常構造）を作れた → findFunctionalGroups に enol 型を新設して通常のアルコールと分離（酸化・脱水・エステル化の対象外に。プロパティ表示は「エノール形のヒドロキシ基（不安定）」）(3) フェノールがエステル化の相手に許可されていた → 除外（直接エステル化は進みにくく、教科書は無水酢酸によるアセチル化。将来ルールで対応）。その他（マルコフニコフの向き・アシル-酸素開裂・同位体キャプション・スルホ6価・splitMolecules・isValencyValid・列挙上限）は妥当と判断。テストL6 — v116
  - [x] **P9-1-M5** 芳香族求電子置換 — ニトロ化（混酸）・スルホン化（濃硫酸・S=6価の-SO₃H）・塩素化（Cl₂/鉄触媒）。置換位置は環炭素をハイライトしてクリック選択（オルト/パラの学習に接続可能）。置換基は outwardSpot で環の外向き（直交に限らず環の角度に沿う方向も探索）に配置。ニトロ基は N(=O)(-O) の電荷分離形。ベンゼン→ニトロベンゼン C₆H₅NO₂／ベンゼンスルホン酸／クロロベンゼンを名称判定で確認。テストL5 — v113
  - [x] **P9-1-M4a** 付加反応とエステルの加水分解 — 多重結合（非芳香族C=C/C≡C）への付加4種: Br₂（臭素水の脱色）・H₂（Ni触媒・硬化油）・HBr・H₂O（酸触媒）。試薬ごとに独立したルールとして提示し、専用パレットは不要にした。水素は明示原子にせず自動水素に任せる（結合次数を1下げるだけで飽和が進む）。片側置換の HBr・H₂O は**マルコフニコフ則**（置換基の多い炭素に X）で主生成物を選択。C≡C は1段階ずつ進行。けん化・加水分解はエステルのアシル-酸素開裂で分割し、アルコール断片を引き離してカルボニルCに-OHを追加（ラクトンは分割しない）。実行時例外では restoreState で開始時状態へ確実に復帰するよう execute を修正。テストL4 — v112
  - [x] **P9-1-M3** 二分子反応の実行 — エステル化（カルボン酸＋別分子のアルコール/フェノール。カルボン酸側の-OHが水として脱離）、分子間脱水（アルコール2分子→エーテル＋水）。相手分子は planAttachment で平行移動して結合位置に寄せる（4方向を試し重なりを回避。空間がなければ実行せず案内）。適用箇所が複数のときは「候補を分けている原子だけをハイライト→クリックで段階的に絞り込む」方式に改良。あわせて名称カードを分子ごとの表示（`酢酸エチル ＋ 水`）に変更し、副生成物のある反応でも生成物名が読めるようにした（splitMolecules / lookupCompoundName）。テストL3 — v111
  - [x] **P9-1-M2** 単分子反応の実行（reactor.js）— 反応ルール5種を実装: 酸化[O]（1級/メタノール型→アルデヒド・2級→ケトン・アルデヒド→カルボン酸[空き直交方向にO追加]・3級は解説のみ表示）、分子内脱水（C-O切断＋α=β二重結合化。脱離したOは分子の外側へ移動し自動Hで水H₂Oとして残る。β候補が複数のときはザイツェフ則で置換の多い側を主生成物に）。カードに適用可能な反応ボタンを列挙し、適用箇所が複数のときはハイライト＋クリック選択。実行=1 Undo・変化箇所ハイライト・教育キャプション付き。エタノール→アセトアルデヒド→酢酸の連鎖と名称判定の連動を確認。テストL2 — v110
  - [x] **P9-1-M1** 「⚗この分子の反応」カード（プロパティ表示＋名称呼び出し）— findFunctionalGroups を chemistry.js に新設（アルコールの級・フェノール性OH・アルデヒド・ケトン・カルボキシ・エステル・エーテル・アミノ・ニトロ・C=C/C≡C・ベンゼン環を検出）し、右パネルに分類をライブ表示（複数分子は【N分子】表記）。名称検索（datalist・全ライブラリ）から分子をキャンバスへ配置（新IDでディープコピー・既存分子の右へグリッド整列配置・1呼び出し=1 Undo）。テストL1 — v109
- [x] **P9-2** 官能基の表示切替（縮約カード）— キャンバス上部の「🔤官能基をまとめる」で、-COOH / -CHO / -NO₂ / -SO₃H を1枚のカードに縮約表示（再クリックで全結合表示へ復帰）。対象は chemistry.js の findCondensableGroups（純粋関数）が検出し、「1つの原子に末端の枝だけがぶら下がる基」に限定（環や主鎖は縮約せず、骨格が消えるギ酸のような分子は対象外）。カードの向きは基が実際に伸びている方向を優先しつつ、既存原子と重なる向きを避けて直交4方向から選ぶ（方向の最適化）。**表示のみの切替**で作図データは不変のため、正解判定・反応実行・エクスポート・名称判定に影響しない。テストO1 — v123
- [x] **P9-3a** 構造異性体の全列挙と学習ビュー（learn.js）— chemistry.js に enumerateConstitutionalIsomers（純粋関数）を新設: 重原子の組成＋水素数から、価標制約つきの連結グラフをDFSで全生成し正準コードで重複除去（次数確定時の非連結枝刈りで高速化。C₄H₁₀O が13ms）。既知の異性体数と一致することを確認（C₄H₁₀O=7 / C₃H₈O=3 / C₄H₁₀=2 / C₅H₁₂=3 / C₄H₈=5 / C₂H₆O=2）。「📚同じ分子式の異性体を調べる」ボタンから、官能基による分類ごとの件数（例: アルコール4種・エーテル3種）、ライブラリ登録名（自分自身は「いま描いている分子」と明示）、書き出し方の4ステップ解説、検出官能基に応じた学習ポイント（級の判定・酸化生成物・銀鏡反応・付加/置換など）を表示。テストM1 — v114
- [x] **P9-3b** 異性体ギャラリー — chemistry.js に自動レイアウト layoutMolecule（純粋関数）を新設: 直交作図コンセプトに沿い、環は長方形/家型テンプレート（findAnyCycle で最初の閉路を検出、3〜8員対応）、鎖は親からの直進優先→直交→距離2倍→螺旋探索のBFS配置。6分子式・全39異性体で原子間24px以上・結合長130px以下を機械検証。学習モーダルの各分類ブロックに構造式サムネイル（自動水素込み・quiz系の renderMoleculeIntoSvg を再利用）を格子表示し、いま描いている分子はシアン枠＋「（この分子）」で強調、登録名はサムネイル下に表示。テストM1拡張（7サムネイル描画・強調枠1個・C₄H₈全異性体のレイアウト非重なり） — v117
- [ ] **P9-3** 分子プロパティ・学習リンク — 作成/呼び出した分子の分類・特徴をプロパティ表示（例: エタノール→1級アルコール）し、そこから関連学習へ誘導（アルコールの酸化反応・級の判定、2-ブタノール→C₄H₁₀Oの構造異性体：アルコール4種＋エーテル3種の全パターン表示と書き出し方の解説）
- [x] **P9-4** 同じ/違うクイズの出題改善（主鎖の屈曲）— 表記変形に屈曲（bend）を追加。単結合の橋を軸に、その先の枝全体を90°回転させて「主鎖が一直線でない」描き方を作る（直交作図のまま曲げるので手書き感覚を保つ）。多重結合を含む枝は sp2/sp の120°/180°作図を壊さないため回さない。候補と回転方向をランダム順に試して重なりを回避し、3パスの打ち消しで直線に戻った場合は最後に1回だけ曲げ直す。強度1で確率0.6・強度2で必ず試行、強度0は原形のまま。検証: 68化合物×20回=1360試行で価標NG0・トポロジー変化0・最小原子間35px、一直線の化合物が78%で屈曲（残りは多重結合のみで直線が正しいもの）。テストM3 — v122
- [x] **P9-6** アニメーション付きチュートリアル（M1〜M3完了につき2026-07-24クローズ） — 設計書: [DESIGN_tutorial.md](DESIGN_tutorial.md)
  - [x] **P9-6-M1** 再生器（tutorial.js: TutorialPlayer）＋FAQモーダル＋3パート（tutorials.json: 原子配置・結合編集・環と縮合）— ❓ヘルプ→一覧・検索（title/summary/keywords部分一致）・デバイス切替（pointer:coarse自動判定＋手動）。実PointerEventで本物のアプリを駆動し、ゴーストカーソル（イージング移動＋クリックパルス）と字幕バーで解説。再生前に作図・履歴・元素選択を退避し終了/中断（✕/Esc）で完全復元。デモの結末は名称判定と連動（エタノール／アセチレン／ナフタレン=この機会にcompounds.jsonへ追加し計54種）。fastモードで回帰テストが全パートを通し再生（座標陳腐化の自動検出）。テストN1 — v119
  - [x] **P9-6-M2** 残り4パート（結合を伸ばす／官能基を付ける／反応を実行する／画面の拡大・移動）＋ホバー導線。新アクション型: summon（名称呼び出し）・reactionButton（反応ボタン＋適用箇所の自動選択）・wheel（Ctrl+ホイールのズーム）・pan（右ドラッグ）。主要ボタン7箇所に `data-tutorial` を付与し、800msホバーで「▶デモを見る」チップを表示（タッチ環境では自然に無効）。**性能問題を1件修正**: 高速モードでもタイマー待機していたため、バックグラウンドタブのタイマー抑制（最大1秒）で7パートの再生が数分に膨張しテストが完走しなかった → `sleep(0)` をマイクロタスク化し、高速モードの全待機を0に。7パート通し再生が**149ms**で完走。テストN1拡張（全7パートの結末: エタノール/C₂H₂/ナフタレン/プロパン/フェノール/エタノール(Undo後)/ベンゼン、ホバー導線の存在確認） — v120
  - [x] **P9-6-M3** 追加2パート（反応機構を見る: モードON→機構選択→ステップ送り→巻矢印と形式電荷の説明／分子を調べる: 異性体ギャラリー・官能基のまとめ表示・立体ビューの案内）と、FAQテキスト項目3件（正しく描いたのに不正解／水素の置き方／保存の可否）をFAQ一覧に同居（`answer` を持つ項目は「答えを見る」で開閉。検索は本文も対象）。新アクション型 toggle（チェックボックス）・select（ドロップダウン）を追加。デモ終了時に反応機構モードと全モーダルを確実に閉じるようにした。全9デモの通し再生は194ms。テストN1拡張 — v125— 入力デバイス別（タッチパネル／マウス／タッチパッド）の操作チュートリアル。実キャンバス上でポインタの動きを再現するデモアニメ＋字幕。呼び出し方は邪魔にならない形: ❓ボタンからのFAQ一覧＋検索、各ボタンのマウスオーバー（長めのhoverで「デモを見る」チップ）、パート単位で個別再生可能。デバイスはpointerメディア特性で自動判定＋手動切替。データはtutorials.json（手順＋座標列）＋tutorial.jsで駆動（回帰テストのイベント駆動基盤を再利用できる見込み）
- [x] **P9-5c** 夜間監査（2026-07-22深夜・v120で5.6時間/5272件）の結果反映 — **実バグ1件を発見・修正**: 表記変形のケクレ位相反転が芳香族結合を一律に反転するため、縮合環（ナフタレン）では環の共有炭素が5本結合になり、クイズで化学的に不正な構造が出題されうる状態だった（v119でナフタレンを追加した翌日に検出）。反転を仮適用して isValencyValid で検査し、妥当な場合のみ採用する方式に修正（単環は従来どおり別のケクレ構造に反転、縮合環では反転せず回転・反転・伸長のみで表記を変える）。縮合系の正しい別ケクレ構造の生成は完全マッチングが必要なため将来課題。残る指摘は乱操作特有の混雑（原子間12〜24pxの接近23件・自動水素の接近36件）のみで、**0.0pxの完全重複と価標超過（ファズ由来）はゼロ**。テストM2 — v121
- [x] **P9-5b** 監査の強化と検出バグの修正（2026-07-22 実施）— ファズに反応実行（M2〜M5）と名称呼び出しを追加、ライブラリ検査に「異性体列挙に自分自身が含まれる」不変条件を追加、価標判定をアプリ本体と共通化（isValencyValid）、反復ごとに履歴をクリアしてシード再現性を確保、失敗シードを再現する診断フック（window.auditRerun）を追加。**発見・修正した実バグ4件**: (1) ニトロ基の -O を別元素に置換すると中心Nが4本結合のまま不正になる → 置換時に隣接原子の妥当性も検査 (2) 芳香族置換の置換基の酸素が衝突判定なしで配置され4pxまで接近 → 置換基をかたまりとして扱い向きを探索 (3) アルデヒド酸化の -OH が空き確認なしで追加され既存原子と完全に重なる（0.0px） → freeSpotAround を使用 (4) 異性体列挙がベンゼン等で5.7秒かかりUIが固まる → 打ち切り上限を調整し「計算中」表示＋描画を譲る非同期実行に変更、学習ビューの対象を重原子6個までに制限。監査の問題件数 92件 → 0〜数件（残りは混み合った分子での自動水素の接近＝表示上の軽微な事象）。テストK4 — v115
- [x] **P9-5** 夜間自動監査ページ（audit.html + audit.js）— ①ライブラリ検査: 全化合物（原形＋強度2変形）を自動作図し、名称対応（同型判定・正準コード）・孤児結合・価標超過（ニトロ特例込み）・原子/自動水素の重なりを検査 ②ランダム操作ファズ: 実イベントでランダム操作（配置・モジュール・次数トグル・切断・削除・伸縮・Undo/Redo）を流し込み同じ不変条件を検査。シード記録付きで失敗は再現可能。結果はJSON保存して後から確認・評価。試運転248件（ライブラリ218＋ファズ30反復）問題0件 — v108

- [x] **P9-7** UIフィードバックの改善（ユーザー実機フィードバック 2026-07-23）— (1) 官能基のカード化で-COOH等の接続線が9pxのスタブになり結合に見えなかった → カード距離を「アンカーからカード手前の辺まで丸1マス」に取り直し、通常の結合と同じ長さ（約31px）の接続線を引くよう修正。(2) 環など混み合った炭素に官能基を作図的に置けない場合、従来は拒否のみだったのを、直交4方向×標準/2倍の結合長で空きを探索して外側に伸ばして配置（完全に塞がれた場合のみ拒否）。getFunctionalGroupPlan を buildAt で方向・距離をパラメータ化。(3) 官能基モジュールと不斉炭素マークモードが排他でなく、handleMouseDownで不斉が優先されてモジュールが置けなかった → 相互排他に（モジュール選択で不斉解除、不斉ON でモジュール解除）。(4) 不斉炭素マークモード中のホバープレビューを追加（カーソル下の炭素に破線リング＋*。実際に不斉炭素ならオレンジ、そうでなければ赤で誤マークを警告、マーク済みはグレー×で解除を示す）。テストP1 — v126

- [x] **P9-8** 縮合環の接合原子への置換基の配置方向を改善（ユーザー実機フィードバック 2026-07-23）— 接合炭素（環結合3本）に炭素/官能基を付ける際、従来は直交4方向[0/90/180/-90]を順に試すフォールバックで、どちらか一方の環に偏った向きになりやすかった → largestGapDirection を新設し「隣接原子がつくる最も広い空き角の二等分線方向」を第一候補にする（直交はフォールバックとして残す）。getFunctionalGroupPlan でも隣接2つ以上なら同じ二等分線を優先し、単純な鎖（隣接0〜1）は従来どおり90°丸め。デカリン接合炭素で置換基が真上（空き角の中央）へ置かれ既存原子と重ならないことを確認。既存の環側鎖ロジック（D4/D5/D6のcyclohexane 2隣接ケース）は不変。テストP2 — v127

### フェーズ10: モード再編とメニュー整理（2026-07-23 ユーザー合意。3モード=学習/パズル/自由）

- [x] **P10** モード再編（M1〜M4完了につき2026-07-24クローズ）— 設計書: [DESIGN_ui_modes.md](DESIGN_ui_modes.md)。散在する機能を3モード（🧩パズル/📚学習/🧪自由）に整理し、上部にモード切替タブ。左パレットは共通、右パネルがモードで切替。不斉マークボタンを左パレットへ移設＋パズルの判定オプションに不斉判定スイッチ。自由モードは白紙の砂場（お題・判定なし）
  - [x] **P10-M4** FAQ「モードとは」を追加 — tutorials.json に faq-modes（パズル/学習/自由の違い・分子はモードをまたいで保持・前回モードの復元）を追加。テストN1拡張（FAQ4件以上＋faq-modes存在） — v144
  - [x] **P10-M3** 自由モードの純化 — 検証の結果、M1（v128）の実装で要件を満了していることを確認: 自由モードの右パネルは「いま描いている分子」「⚗この分子の反応（分析ツール: 異性体・立体・名称呼び出しを含む）」のみで、お題・判定・ステージ選択は非表示。モードのlocalStorage保存もv128で実装済み。ヒントボックスは全モード共通の操作説明のため存置 — 追加実装なし
  - [x] **P10-M2** 不斉炭素の扱いを再配置（#3・#4）— 兼用だった asymmetricMode を分離: (1) 不斉マーク編集モードを左パレット「操作モード」の✳ボタン（btn-asym-mark）へ移設。Select/Bond/Eraseと排他（ツール選択で解除）。(2) 判定に含めるかを、パズルの新設「判定オプション」枠のスイッチ check-judge-asymmetric（judgeAsymmetric）で制御。verifyCurrentStructure は judgeAsymmetric を参照。修正: btn-asym-markがtool-btnクラスを持つためtool-btnハンドラと二重発火し自身のactiveを消していた → ツール系の処理を .tool-btn[data-tool] に限定。テストP3、既存G4/P1を新方式へ更新 — v130
  - [x] **P10-M1** モード切替タブと右パネルの3分割 — 上部に🧩パズル/📚学習/🧪自由タブ。右パネルの各ブロックにdata-modes属性を付け、setMode(mode)で表示/非表示を切替（パズル=ステージ選択・お題・判定／学習=クイズ・命名・反応機構ビューア／自由=反応カード・異性体・立体・名称呼び出し。いま描いている分子カードはパズル+自由、判定結果/トースト先のverify-resultは全モード共通で常設）。作図中の分子はモード切替で保持。学習を離れると反応機構モードを終了。モードはlocalStorageに保存し次回復元。左パレット（作図の道具）は共通。テストQ1 — v128

### フェーズ11: スマホ対応（レスポンシブ）（2026-07-24 ユーザー合意。縦横両対応・設計書先行）

- [x] **P11** スマホ対応（M1〜M3f完了・実機確認済みにつき2026-07-24クローズ）— 設計書: [DESIGN_mobile.md](DESIGN_mobile.md)（作成済み・実装はユーザー確認後）。キャンバス最優先で、ツール・お題パネルをドロワー/下シート化。ブレークポイント min-width:900px=デスクトップ現状維持、899px以下は向き別レイアウト（縦=キャンバス上・ツール下バー・右パネル下シート／横=圧縮3カラム）。CSS中心＋最小限のJS、座標はgetScreenCTM依存で自動追従。M2:横レイアウト / M3:仕上げ（タップ領域・モーダル・チュートリアル）。
  - [x] **P11-M3f** ☰お題・設定をリボン列に統合（実機フィードバック: 固定レーン方式だと環境の dvh 差で数px不足しスクロールバーが出て鬱陶しい）— ☰ボタンのDOMを canvas-header 内へ移動し、横向きでは position:static でリボン列の最下段に通常フローで流し込む（固定64pxレーンを廃止し bottom:8px までを列が使う）。縦ではfixed（bottom:148px右下の浮動ボタン）・PCでは非表示のまま＝DOM位置は無関係。あふれた場合も scrollbar-width:none / ::-webkit-scrollbar で棒は出さない（スクロール自体は可能）。812x300で☰込み1列・オーバーフロー1px・バー非表示・開閉動作を実測。※検証時の注意: Browserペインはリサイズ直後にカスケード再評価が遅れることがある（縦向きの bottom:148 が一時 12px と誤計測 → リロードで正常）。実機の画面回転では発生しない — v143
  - [x] **P11-M3e** 横リボンの2列化是正とスリム化（実機フィードバック）— 実機でリボンが2列になる原因は、共通ブロックの flex-wrap:wrap が縦列にも効き、アドレスバーで列の高さが不足すると折り返して2列目を作っていたこと（フル高さの検証環境では再現せず）。flex-wrap:nowrap で1列固定にし、はみ出しは縦スクロール。ボタンをスリム化（min-height 34px・padding 5px 8px・gap 4px。☰は38px）。「官能基をまとめる」は <wbr> をやめ全角スペース（官能基を　まとめる）で折返し位置＋見た目を両立。812x300（アドレスバー相当）で1列・スクロール・☰非重なりを実測 — v142
  - [x] **P11-M3d** 横リボンの重なり是正と改行固定（実機フィードバック）— (1) ☰お題・設定が全消去と重なる問題: リボン列に bottom:64px の下端制約を付けて☰専用レーンを確保（アドレスバーで dvh が縮んでも両者は同時に追従するため重ならない。レーン不足の極低画面はリボン側をスクロール）。☰は幅108px・右端でリボン列の最下段にそろえた。(2) 「官能基をまとめる」の改行位置はCJK任意折返しでフォント環境依存だった → ラベルに <wbr>（官能基を|まとめる）を入れ word-break:keep-all で「<wbr>以外では折らない」ようにし、全環境で同じ2行に固定 — v141
  - [x] **P11-M3c** 横リボンの縦列化＋化合物名チップ（実機フィードバック）— (1) 横向きのオーバーレイボタン群を上の横並びから「右端に幅108px統一の縦積み」へ変更（アドレスバーで縦が貴重なため。❓フロートの下から開始。「官能基をまとめる」は2行折返し）。(2) モバイル両向きでキャンバス左下に化合物名チップを常設: syncMobileNameChip が右パネルの「いま描いている分子」と同期し「名称＋分子式」（名称なしは分子式のみ）を表示。学習モード・空分子では消える（:empty で非表示）。setMode/updateCompoundInfo の全分岐で同期。PCは .mobile-only で非干渉。テストR7 — v140
  - [x] **P11-M3b** 横画面の縦幅確保（実機フィードバック: 横だと縦が狭い）— 横向き限定で (1) ロゴバー（ヘッダー44px）を廃してヘッダーを absolute 化、❓ヘルプだけ右上にフロート表示（ロゴ非表示・ドロワーは top:0 に）(2) キャンバス上部リボン（↩↪/まとめ/全体表示/全消去）を半透明の absolute オーバーレイとしてキャンバスに重ねる（隙間は pointer-events:none でキャンバス操作を透過、ボタンだけ受ける）。座標表示は非表示。キャンバス実測 250px→361px（+45%）。縦・PCは非干渉を実測確認。テストR6 — v139
  - [x] **P11-M3** 仕上げ（タップ領域・モーダル・チュートリアル連動）— (1) タップ領域44px基準: 下ツールバー/横ツール列のボタン44px・シート開閉トグル45px・閉じるボタン44px・モードタブ45px・キャンバス上部ボタン42px（❓ヘルプのみヘッダー44px制約で34px）。⤓JSONはコンテンツ制作用のためモバイルでは非表示（DOM存置）。(2) モーダル: 余白を18pxに圧縮して内容幅を確保。FAQの検索欄が flex min-width:auto で縮まずデバイス選択が5pxはみ出していたのを min-width:0 で修正。learn/quiz/stereo は375px幅で横はみ出しなしを実測確認。(3) チュートリアルのシート連動: デモが右パネル内の要素（summon・反応ボタン・トグル等）を操作する前にシートを自動で開き、キャンバス操作の前に閉じる（setSheetOpen/syncSheetFor。PC≥900pxでは無効）。終了時は必ず閉じる。375px幅で reaction/mechanism デモの通し再生エラーなしを確認。テストR5 — v138
  - [x] **P11-M2d** 2本指ドラッグでキャンバスをパン（ユーザー質問で発覚: スマホに平行移動の操作が存在しなかった。ピンチは中点固定のズームのみ・右ドラッグパンはPC専用）— ピンチ処理をアンカー方式に変更: 開始時に2本指の中点の下にあった論理座標(anchor)を記録し、移動中は「新しい倍率を先に適用 → getScreenCTMで現在の中点の論理座標を取得 → anchorとのずれ分だけviewBoxを平行移動」。指の間隔の変化=ズーム・中点の移動=パンが同時に効く。中点が動かない場合は従来のズーム挙動と同一。テストI2（純パンで倍率不変・方向・続けてピンチ・誤配置なし） — v137
  - [x] **P11-M2c** Undo/Redoボタンをキャンバス上部に常設（ユーザー要望: スマホにキーボードがなく Ctrl+Z が使えない）— canvas-header に「↩ 戻す」「↪ やり直し」を追加（PC/モバイル共通。既存 undo()/redo() を呼ぶだけ）。分子分裂トーストの案内文も「↩ 戻す（Ctrl+Z）」に更新。付随して発見・修正したモバイルレイアウト2件: (1) canvas-header は基本CSSで高さ40px固定のため、折り返した2行目がキャンバスに重なっていた → モバイルでは height:auto。(2) 横向きで svg-wrapper がSVGの縦横比由来の固有高さ（812px幅で483px）を min-height:auto として保持し、キャンバス下部約40%がクリップされていた → min-height:0（M2からの潜在問題）。テストR4 — v136
  - [x] **P11-M2b** モバイルの操作ボタン削減（ユーザー合意: チュートリアル・初回ヒントで代替）— モバイル（≤899px）で Select ボタンと結合タイプ枠（単/二重/三重）を非表示に。DOMには残す（PC表示・チュートリアル・テストのため）。根拠: 結合次数は結合線タップのトグルで変更可能（C9）、Select は既定モードで原子/モジュール選択時に自動復帰。代替導線: (1) アクティブツールの再タップで Select へ復帰（setTool へ集約。結合次数ボタンは .click() でなく直接 setTool('bond')—連打での解除発火を防止）(2) 初回の結合作成時に一度だけ「結合線タップで次数変更」ヒントをトースト表示（localStorage: chemHintBondToggle）(3) チュートリアルのゴースト再生は非表示ボタン（rect=0）をカーソル演出なしでクリック実行。showToast のタイマーが後続の別メッセージを消さないよう修正。Eraser・Draw Bond・✳ は存置（削除の非常口／既存原子同士を結ぶ唯一の手段／不斉マークの唯一の入力）。左列スクロール高 908→716px。テストR3 — v134
  - [x] **P11-M2** モバイル横レイアウト（圧縮3カラム）— モバイルCSSを「縦横共通/(orientation:portrait)/(orientation:landscape)」の3ブロックに再構成。横向きは: 左パレット=幅148pxの縦スクロール列（原子ボタンは font-size:0 で記号の丸のみ・和名ラベル非表示）、キャンバス中央、右パネル=右からのドロワー（translateX。既存の body.sheet-open 開閉機構を流用しJS変更なし）。html に text-size-adjust:100% を追加（モバイルの自動文字拡大が指定フォントサイズを 10.5px→14px に壊すのを実測・防止）。PC・縦レイアウトは非干渉を確認。テストR2（向き別メディアクエリのCSSOM検査）— v133
  - [x] **P11-M1** モバイル縦レイアウト — max-width:899px のメディアクエリで、main を縦積みに: キャンバスを最大化（flex:1、375px幅で高さ618px確保）、左パレットを最下部の横スクロールツールバー（flex-direction:row）、右パネルをposition:fixed の下シート（transform:translateY で開閉、body.sheet-open で表示）。開閉トグル（浮動ボタン）・閉じるボタン・バックドロップを追加（.mobile-only、PCでは display:none）。100dvhでアドレスバー追従、モーダルはmax-height:90dvh。PC（≥900px）は完全非干渉を確認。テストR1 — v131
  - [x] **P11-M1b** モバイル下ツールバーの是正（実機フィードバック）— 各グループのボタンが折り返して縦に伸び、バー高132pxで下段が隠れていた → 各パレット（操作モード/結合/原子/モジュール）を横1列（flex-wrap:nowrap）にし、バーは横スクロールで到達。高さ82pxに圧縮しキャンバスを拡大。SO₃H追加で9モジュールでも縦の切れなし — v132PCは非干渉（メディアクエリ内に隔離）

- [ ] **P9-5e** 夜間監査（2026-07-23・5272件中40件）のフォロー — 全件が「乱操作特有の接近」クラスで価標超過・孤児結合・0.0px完全重複はゼロ。v138でのシード再現により2つの発生機構を特定（5シード中2件が再現。残りは挙動変更で操作軌跡が変わったため）: (1) **自動水素12px級**（Br付近に頻出）= 非結合原子が配置許容の下限（MIN_CLEARANCE 27.3px）ぎりぎり28pxに合法配置され、隣の原子の自動水素（結合長16px）がその方向を向くと 28−16=12px になる境界幾何。seed 2418435272 で再現。緩和案: 混雑方向の水素を短縮描画/さらに回避。(2) **原子14〜23px級** = 反応実行・モジュール配置の組合せで非結合原子が14pxまで接近する経路が残っている（座標が非グリッドの反応生成物）。seed 2418434542 で再現。reactor の配置クリアランスの統一を点検。いずれも視覚的混雑でデータ破壊なし。修正は原因明白につき Opus 向け作業

### フェーズ12: コンテンツ拡充と反応の可視化（2026-07-24 ユーザー要望登録）

- [ ] **P12-1** 異性体の書き出し練習モード — 設計書: [DESIGN_isomer_practice.md](DESIGN_isomer_practice.md)（2026-07-24 作成、同日ユーザーレビュー反映済み: ①登録構造は必ず命名して表示（重複時は「◯番『名称』と同じ」）②未発見構造から系統的にヒント生成（系列内訳→書き出し手順の定型文「エチル基/メチル×2同一C/メチル×2別C」→答え）③答え合わせは系統順の一覧（isomerSeriesKey でソート）④非正準な書き方の検出時は主鎖をハイライト＋標準レイアウト並置（findLongestCarbonChain）。**実装は Opus で行う**（M1から順に。設計書に実装手順・テスト項目記載済み）。学習モードの一機能として、分子式を提示→構造異性体を1つずつ描いて登録→重複は正準コードで検知し該当サムネイルをハイライト（表記の同値性の実践）→全種そろえて完了。問題6問（C₄H₁₀ 2種〜C₄H₁₀O 7種、異性体数は列挙エンジンから計算し二重管理しない）。新規アルゴリズム不要（列挙・正準コード・ギャラリー描画を流用）。M1: コアループ / M2: 段階ヒント・完了演出 / M3: 任意分子式（P12-2連動）
  - [x] **P12-1-M1** コアループ — learn.js に IsomerPractice クラスを追加（学習パネルに「✏️ 異性体の書き出し練習」カード）。問題6問（C₄H₁₀〜C₄H₁₀O。異性体数は列挙エンジンから算出）。登録/重複/分子式・単一分子チェックを canonicalCode で判定し、名称付きサムネイルをトレイに表示。全種そろえて完了→クリア記録（localStorage: chemIsomerPractice.<分子式>）＋系統順（主鎖の長い順・カテゴリ順）の答え合わせ一覧＋分類まとめ。ヒントは残り数のみ（段階ヒントは M2）。登録時はキャンバスを saveState してから白紙化（↩で復帰）。学習モード離脱で練習を強制終了（setMode フック）。**C₆H₁₄ は既定の列挙上限60万ノードを超えるため練習では上限400万で列挙**（設計の「高速ケース」想定を実測で更新。6問で計~170ms・一度だけキャッシュ）。compounds.json に不足していた7異性体名（エチルメチルエーテル・1-ブテン・2-メチルプロペン・メチルシクロプロパン・メチルプロピルエーテル・イソプロピルメチルエーテル・2-メチル-1-プロパノール）を追補し、6問25異性体すべてが名称解決することをテストIP4で機械検証。テスト IP1（2種登録して完了・名称・クリア記録）/ IP2（重複拒否＋点滅）/ IP3（分子式違い・複数分子の拒否）/ IP4（25種の命名網羅＋列挙数の既知値一致）。全80テスト合格 — v145
- [ ] **P12-2** 異性体列挙の対応範囲拡張 — 現状は重原子6個まで・不飽和度が高い分子式は打ち切り。列挙アルゴリズムの高速化（正準コードの重複除去の効率化・枝刈り強化）や、環/多重結合を含む分子式への対応範囲を広げる
- [ ] **P12-3** 命名可能化合物の拡張（P8-6の継続）— compounds.json をさらに拡充。系統名（IUPAC）アルゴリズムの検討も
- [ ] **P12-4** 高校化学の教科書反応の網羅（P8-7の継続）— 反応機構データ（reactions.json）と反応実行ルール（reactor.js）の両面で、教科書定番の反応を一通りそろえる。残り候補: アルコールの酸化の機構版・エタノールの分子間脱水・アニリンのジアゾ化とカップリング ほか
- [ ] **P12-5** 自由モードの反応実行にアニメーションを追加 — 現状は結果だけ表示。反応前→反応後の変化（原子移動・結合の組換え）をモーフィングで見せる（reaction.jsの状態補間ロジックを流用。DESIGN_reaction_execution.md の簡易版アニメの実装）。要設計
- [ ] **P12-6**（P10検討残り）まとめ表示中の官能基編集 — カード化した官能基を編集する手段（案: カードをタップして一時展開／別の官能基へ変換）。要方針決定

---

## 9. 本方針の変更

方針自体の変更もコミットとして記録する。ロードマップの完了チェック・項目追加は随時行ってよいが、第1〜7章のルール変更は理由をコミットメッセージに残す。
